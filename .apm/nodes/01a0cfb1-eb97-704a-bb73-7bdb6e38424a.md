## Intent

- `max_concurrent` in total and one active run per node (extra runs rejected, not queued). Cancel: SIGTERM, then SIGKILL after 5s. Resume via `--resume <session>`.

## Acceptance criteria

- [ ] fake-`claude` tests for both limits, cancel of a script that ignores SIGTERM, and resume arguments

## Notes

Plan: plans.md §12.3, P11.3.
