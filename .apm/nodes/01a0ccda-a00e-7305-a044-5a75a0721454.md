## Intent

`apm serve`: runner, decomposer + critic, proposals, SQLite runs

## Acceptance criteria

- [ ] `apm run apm-decomposer <h>` → proposal → `apm proposal accept` → one commit; the three M3 CLI checks in §7.1 are answered

## Notes

Plan: plans.md §11.
