## Intent

- `apm_create_nodes` (batch, one commit), `apm_update_node`, `apm_move_node`, `apm_link` / `apm_unlink`, `apm_set_status`, `apm_attach_delivery` (leaves only).

## Acceptance criteria

- [ ] each tool produces exactly one commit
- [ ] invalid input returns a tool error naming the invariant

## Notes

Plan: plans.md §12.3, P08.2.
