## Intent

Node 24 strips types natively, so the code keeps to erasable TypeScript and imports `.ts` files; `tsc -b` builds `dist/` only for publishing.

## Acceptance criteria

## Notes

Decided in P01 (PR #2). Plan: plans.md §12.3, P01.
