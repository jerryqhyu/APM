## Intent

- Cycle detection on each level's lifted edges, reporting the nodes in the cycle. `checkAll()` runs `validate()` plus I5 at every level; it backs `apm check`.

## Acceptance criteria

- [ ] an entangled fixture is reported at the right level only
- [ ] a clean generated graph with no lifted cycles reports none

## Notes

Plan: plans.md §12.3, P04.3.
