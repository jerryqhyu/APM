## Intent

`graph.ndjson` (structure + metadata) + `nodes/<id>.md` (prose only)

## Acceptance criteria

## Notes

Why: Each field lives in exactly one place, so the two can't drift

Plan: plans.md §1.
