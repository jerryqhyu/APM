## Intent

- The `apm-reconciler` agent (scope `project`), started for the cases `apm reconcile` reports as ambiguous.

## Acceptance criteria

- [ ] a real merge moves its node to `done` with no manual step

## Notes

Plan: plans.md §12.3, P19.2.
