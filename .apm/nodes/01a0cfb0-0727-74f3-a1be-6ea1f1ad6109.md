## Intent

- `nodes_fts` over title + body, kept current by the same indexing path; `search(query)` returning ranked node IDs.

## Acceptance criteria

- [ ] search finds words from titles and bodies, and a body edited after indexing is found by its new text only

## Notes

Plan: plans.md §12.3, P06.3.
