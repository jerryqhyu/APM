## Intent

- `read`, `node:<id>` and `project` scopes, checked before any tool runs; an unknown scope refuses every write.

## Acceptance criteria

- [ ] allowed and refused calls tested for every scope and tool

## Notes

Plan: plans.md §12.3, P08.3.
