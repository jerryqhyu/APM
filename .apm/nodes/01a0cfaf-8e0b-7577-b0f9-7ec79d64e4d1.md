## Intent

- Typed op functions on top of `mutate()`, each one commit with the §3.5 subject and trailers. `create` writes the body template.

## Acceptance criteria

- [ ] one test per op checking the graph, the body file and the commit message

## Notes

Plan: plans.md §12.3, P05.5.
