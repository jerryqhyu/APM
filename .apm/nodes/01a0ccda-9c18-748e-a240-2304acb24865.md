## Intent

`core.mutate()` under a file lock, callable from any process

## Acceptance criteria

## Notes

Why: No daemon required; writes happen one at a time, so updates aren't lost

Plan: plans.md §1.
