## Intent

- `node add`, `show`, `mv`, `rm`, `status`; `link-dep`, `unlink-dep`.

## Acceptance criteria

- [ ] e2e test of each command, checking the resulting commit
- [ ] invariant violations exit non-zero with the invariant named

## Notes

Plan: plans.md §12.3, P07.2.
