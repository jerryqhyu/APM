## Intent

## Acceptance criteria

- [ ] fixture tests cover bad keys, cycles introduced by a proposal (rejected) and additive-only violations. A real run of `apm run apm-decomposer <h>` → `apm proposal accept` produces exactly one commit

## Notes

Plan: plans.md §12.3, P12.
