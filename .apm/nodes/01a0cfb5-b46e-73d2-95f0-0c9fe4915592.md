## Intent

- Trailer scan plus `gh pr view` → status updates, as one commit with `APM-Actor: reconciler`. Runs manually, and once when `apm serve` starts.

## Acceptance criteria

- [ ] tests with a stubbed `gh`: merged → `done`, open → unchanged, closed unmerged → reported, not changed

## Notes

Plan: plans.md §12.3, P19.1.
