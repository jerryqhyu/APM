## Intent

- A lazy blob loader with an LRU of parsed graphs; `GET /api/frames`; `GET /api/graph?at=<sha>`; per-node body history.

## Acceptance criteria

- [ ] `?at=` returns the historical level view
- [ ] LRU eviction test

## Notes

Plan: plans.md §12.3, P16.2.
