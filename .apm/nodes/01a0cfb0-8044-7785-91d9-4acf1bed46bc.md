## Intent

- Thin git wrappers; `link --remote` records the remote in `.apm-link`, and `clone` reads it on a fresh checkout.

## Acceptance criteria

- [ ] e2e against a bare temp repo as the remote: link → push → clone elsewhere → pull

## Notes

Plan: plans.md §12.3, P07.5.
