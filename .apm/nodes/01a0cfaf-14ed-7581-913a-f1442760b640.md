## Intent

- Edges with one end outside `root` become incoming or outgoing ports, each naming the outside node and the inside child it attaches to.

## Acceptance criteria

- [ ] port fixtures (including the top level, which has none)
- [ ] the exactly-once property test over `arbGraph()`

## Notes

Plan: plans.md §12.3, P04.2.
