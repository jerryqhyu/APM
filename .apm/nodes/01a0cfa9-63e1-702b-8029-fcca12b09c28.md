## Intent

Unknown keys are errors. Hand-written lines may use any key order, `null`s, a byte-order mark and omit `kind`/`status`; the next write canonicalizes them. `delivery.pr` is a URL.

## Acceptance criteria

## Notes

Decided in P02.4 and P02.5 (PRs #7, #8). Plan: plans.md §3.2.
