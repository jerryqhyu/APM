## Intent

- The `proposals` table; decomposer → critic → at most one revision → a `proposals` row → WS `proposal.created`.

## Acceptance criteria

- [ ] fake-`claude` tests for a clean pass, one revision, a critic that still objects after the revision, and a failed run (no proposal)

## Notes

Plan: plans.md §12.3, P12.2.
