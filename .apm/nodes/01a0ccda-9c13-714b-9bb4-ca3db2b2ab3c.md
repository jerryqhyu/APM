## Intent

Separate git repo at `.apm/`, ignored by the code repo and linked to it by a committed `.apm-link`

## Acceptance criteria

## Notes

Why: No CI runs on plan commits; works with a protected `main`; history holds only plan changes

Plan: plans.md §1.
